# ── Core server runtime ────────────────────────────────────────────────
fastapi==0.128.0
uvicorn[standard]==0.40.0
websockets==16.0

# ── Numerical / imaging ───────────────────────────────────────────────
numpy==2.4.1
opencv-python==4.13.0.90
pillow==12.1.0

# ── Deep learning (SigLIP2 + DINOv2 ensemble) ─────────────────────────
# NOTE: on machines WITHOUT an NVIDIA GPU (e.g. the Intel Core Ultra /
# Arc iGPU), install the smaller CPU-only wheel to save ~2GB:
#   pip install torch==2.13.0 torchvision==0.28.0 --index-url https://download.pytorch.org/whl/cpu
#
# OPTIONAL — Intel Arc GPU (XPU) acceleration via Intel Extension for PyTorch.
# Gives a big speedup on Intel Core Ultra / Arc laptops. IPEX must match your
# torch version exactly, so install torch+torchvision from Intel's XPU index,
# then IPEX:
#   python -m pip install torch torchvision --index-url https://pytorch-extension.intel.com/release-whl/stable-xpu/us/
#   python -m pip install intel-extension-for-pytorch
# The server auto-detects XPU if IPEX is installed; otherwise it falls back to CPU.
torch==2.13.0
torchvision==0.28.0
transformers==4.57.6
timm==1.0.28
peft==0.19.1
huggingface_hub==0.36.2

# ── Classical ML (Random Forest classifier) ───────────────────────────
scikit-learn==1.8.0

# ── Optional: OCR (disabled by default; enable with MONET_OCR=1) ───────
# Also requires the Tesseract engine installed on the OS:
#   macOS:  brew install tesseract
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   Linux:  sudo apt install tesseract-ocr
pytesseract==0.3.13
